# В репозитории уже есть .env; при необходимости скопируй этот файл как шаблон.
PORT=3001
HOSTNAME=0.0.0.0
# Origin фронтенда (Vite dev)
CORS_ORIGIN=http://localhost:3000
# Каталог для lowdb (по умолчанию ./data от cwd)
# DATA_DIR=
