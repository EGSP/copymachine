Состояние валидации (false) - выделяется красным `border`
Должна быть функция валидации в props
- Если функция не передана, или не придумана, то по возможности
должны быть предопределенные функции, которые можно вписать в пропс.

В `readonly` не должно быть `border`

